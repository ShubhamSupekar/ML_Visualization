MlVisio is the envirnoment created for this project 
Install Miniconda
to activate the envirnoment run below command:
conda env create -f environment.yml
Windows
  python src\app.py
Unix
  python src/app.py
